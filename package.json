{
  "name": "care-scheduler",
  "version": "1.0.0",
  "description": "Care Scheduler Application",
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"cd client && npm i && npm run build\" \"cd server && npm i && npm run dev\"",
    "heroku-postbuild": "cd client && rm -rf package-lock.json node_modules && npm install --omit=dev false && npm run build && cd ../server && npm ci"
  },
  "dependencies": {
    "cloudinary": "^1.41.3",
    "concurrently": "^7.6.0",
    "multer-storage-cloudinary": "^4.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4"
  }
}
