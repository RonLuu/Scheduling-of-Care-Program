{
  "name": "care-scheduler",
  "version": "1.0.0",
  "description": "Care Scheduler MERN Application",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "cd server && npm install && node index.js",
    "dev": "concurrently \"cd client && npm install && npm run build\" \"cd server && npm install && npm run dev\"",
    "heroku-postbuild": "cd client && npm install && npm run build && cd ../server && npm install"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
