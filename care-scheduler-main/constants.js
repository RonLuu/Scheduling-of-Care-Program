const client_port = 3000
const client_URL_users = "http://localhost:5000/api/users"
const server_port = 5000
const server_URL = "http://localhost:${server_port}/"
export {client_port, server_port, client_URL_users}